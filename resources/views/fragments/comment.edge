@if(comment.id)
  <div
    hx-swap-oob="afterbegin:#{{ comment.commentableType === 'Activity' ? 'comments' : `comment_${comment.commentableId}_replies` }}"
  >
    @!comments.comment({comment})
  </div>
@end
