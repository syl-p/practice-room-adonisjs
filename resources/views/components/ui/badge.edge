@let(node = $props.has('href') ? "a" : "span")
@let(classNames = "inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2")

<{{node}} {{$props.merge({
  class: [classNames]
}).toAttrs()}}>
{{{await $slots.main()}}}
</{{node}}>
