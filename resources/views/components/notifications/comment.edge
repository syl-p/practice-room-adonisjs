<a href="{{ notification.data.href }}" class="flex w-full items-center space-x-3 px-4 py-2">
  <div>
    @!users.avatar({user: notification.data.comment.user})
  </div>
  <div class="flex-1">
    <p class="block font-sans text-sm font-normal leading-normal text-gray-700 antialiased mb-1">
      <span class="font-medium text-blue-gray-900">{{ notification.data.comment.user.username }}</span>&nbsp;{{ notification.data.message }}
    </p>
    <p class="flex items-center gap-1 font-sans text-xs font-light text-gray-600 antialiased">
      @svg('heroicons:clock', {class: "h-4 w-4 text-gray-400"})
      {{ utils.timeago(notification.createdAt) }}
    </p>
  </div>
</a>
